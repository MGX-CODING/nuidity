import { NuiButtonDirective } from './button.directive';

describe('ButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new NuiButtonDirective();
    expect(directive).toBeTruthy();
  });
});
